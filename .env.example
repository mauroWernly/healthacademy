DATABASE_URL="postgresql://healthacademy:healthacademy_dev@localhost:5432/healthacademy?schema=public"

# Auth.js — generate with `npx auth secret` for a real deployment
AUTH_SECRET="replace-with-a-strong-random-secret"

# File storage: "local" | "s3"
STORAGE_DRIVER="local"
STORAGE_LOCAL_PATH="./storage"
